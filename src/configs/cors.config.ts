export const CORS_CONFIG = {
  // TODO: update this with the client URL
  // (or implement a function to dynamically set the origin see: https://github.com/expressjs/cors#configuration-options)
  origin: ['http://localhost:4200'],
  optionsSuccessStatus: 200,
};
